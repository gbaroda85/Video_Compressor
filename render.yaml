services:
  - type: web
    name: videotools-api
    env: node
    region: singapore
    plan: free
    buildCommand: "npm install -g pnpm && pnpm install && pnpm --filter @workspace/api-server run build"
    startCommand: "node --enable-source-maps ./artifacts/api-server/dist/index.mjs"
    envVars:
      - key: NODE_ENV
        value: production
